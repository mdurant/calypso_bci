<%= render "layouts/menu_sin_login"%>
<div class="container">
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <div class="well fondo-form">
        <%= form_for(@category) do |f| %>
         <% if @category.errors.any? %>
            <div id="error_explanation">
            <h2><%= pluralize(@category.errors.count, "error") %> prohibited this deed from being saved:</h2>
            <ul>
            <% @category.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
          </div>
          <% end %>
          <div class="form-group">
            <%= f.label :title, "Titulo" %><br>
            <%= f.text_field :title, class:"form-control" %>
          </div>
          <div class="form-group">
            <%= f.label :description, "Descripción" %><br>
            <%= f.text_field :description, class:"form-control" %>
          </div>
          <div class="row pull-right">
            <div class="col-md-12">
              <div class="actions form-group">
                <%= f.submit "Actualizar Categoria", class: "btn btn-success" %>
              </div>    
            </div>
          </div>
      </div>
    </div>
  </div>
</div>


<% end %>
